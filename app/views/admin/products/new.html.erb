<% provide :title, t("admin.products.createproduct") %>
<h1><%= t "admin.products.createproduct" %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @product], html: {multipart: true} do |f| %>

      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :classify %>
        <div class="checkbox">
          <label>
            <%= radio_button "product", "classify", "food" %>
            <%= t "food" %>
          </label>
        </div>
        <div class="checkbox">
          <label>
            <%= radio_button "product", "classify", "drink" %>
            <%= t "drink" %>
          </label>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :category %>
        <%= f.select :category_id, @categories.collect{|p| [p.name, p.id]},
          {include_blank: false}, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :price_tag %>
        <%= f.number_field :price_tag, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :quantity %>
        <%= f.number_field :quantity, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.file_field :image, accept: "image/jpeg, image/gif, image/png" %>
      </div>

      <%= f.submit t(:create), class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
